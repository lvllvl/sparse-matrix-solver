#include "SparseMatrixGenerator.hpp"
#include <random> // For random number generation

Matrix generateSparseMatrix(int m, int n, double s) {
    Matrix matrix(m, std::vector<double>(n, 0.0));

    // ... rest of the logic for generating the sparse matrix ...

    return matrix;
}
